---
site:
  domain: ibitrade.ibi.co.il
  subdomain: ibitrade
  # alias: domain.com
  root: trade
  username: ibi_site
  caracal_branch: v0.3.x
  # ip_address: 0.0.0.0
  # owner: Some Person
  # plan: Unlimited
  # database_type: 'mysql'

local_site_directory: ../site
subscription_directory: "{{ plesk_vhost_directory }}/{{ site.domain }}"
site_directory: "{{ subscription_directory }}/{{ site.root }}"
